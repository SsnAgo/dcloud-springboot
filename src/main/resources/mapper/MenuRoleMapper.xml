<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dcloud.mapper.MenuRoleMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.dcloud.pojo.MenuRole">
        <!--@Table t_menu_role-->
        <id column="id" property="id"/>
        <result column="mid" property="mid"/>
        <result column="rid" property="rid"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        mid,
        rid
    </sql>
    <!--auto generated by MybatisCodeHelper on 2021-03-31-->
    <select id="getAllByRid" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_menu_role
        where rid = #{rid}
    </select>

    <select id="getMenusByRoleId" resultType="com.example.dcloud.pojo.Menu">
        select m.*
        from t_menu_role mr,
             t_menu m
        where mr.rid = #{rid}
          and mr.mid = m.id
        order by m.id
    </select>

    <insert id="insertRecords">
        insert into t_menu_role(mid, rid)
        VALUES
        <foreach collection="mids" item="mid" separator=",">
            (#{mid},#{rid})
    </foreach>
    </insert>
</mapper>

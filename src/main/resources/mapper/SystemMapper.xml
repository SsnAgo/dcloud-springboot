<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dcloud.mapper.SystemMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.dcloud.pojo.System">
<!--    @Table t_system-->
        <id column="id" property="id" />
        <result column="signExp" property="signExp" />
        <result column="signDistance" property="signDistance" />
        <result column="enabled" property="enabled" />
        <result column="lv" property="lv" />
        <result column="left" property="left" />
        <result column="right" property="right" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, signExp, signDistance, enabled, lv, left, right
    </sql>

</mapper>
